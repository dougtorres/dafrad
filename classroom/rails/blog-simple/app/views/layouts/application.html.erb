<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>BlogSimple</title>
    <link rel="alternate" type="application/rss+xml" title="Codingfish RSS Feed" href="http://localhost/feed?format=rss">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-fixed-top">
       <div class="container">
         <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Meu Blog', articles_path, class: "navbar-brand" %>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if current_page?(root_path) or current_page?(articles_path) %>">
              <%= link_to 'Blog', articles_path %>
            </li>
            <% if current_user %>
              <li class="<%= 'active' if current_page?(new_article_path) %>">
                <%= link_to 'Create Article', new_article_path %>
              </li>
              <li>
                <%= link_to 'Logout', logout_path %>
              </li>
            <% else %>
              <li class="<%= 'active' if current_page?(signup_path) %>">
                <%= link_to 'Sign up', signup_path %>
              </li>
              <li class="<%= 'active' if current_page?(login_path) %>">
                <%= link_to 'Login', login_path %>
              </li>
            <% end %>
          </ul>
        </div>
       </div>
    </nav>
    <div class="container" style="margin-top: 60px">
      <% unless flash[:notice].nil? %>
        <!-- <div class="alert alert-success"><%= flash[:notice] %></div> -->
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% unless flash[:error].nil? %>
        <div class="alert alert-danger"><%= error %></div>
      <% end %>
      <%= yield %>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </body>
</html>
